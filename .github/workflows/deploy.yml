name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: shahidalam7786_sa
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /home/shahidalam7786_sa/Shahid-Creatives-Bot
            git stash
            git pull origin main
            npm install
            pm2 restart all
